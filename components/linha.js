import styles from '../styles/portfolio.module.css';
import Link from 'next/link';
import Button from 'react-bootstrap/Button';

export default function Linha(props){
    const coin = props.props;
    console.log(coin);
    return (
        <div>
            <h1>Refatorar linha</h1>
        </div>
    )
}

